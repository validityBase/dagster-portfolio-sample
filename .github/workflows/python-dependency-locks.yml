name: Python Dependency Locks

on:
  pull_request:
  push:
    branches: ["**"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dependency-locks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # actions/checkout v5.0.1

      - name: Install lock tooling
        uses: validityBase/vbase-github-actions/.github/actions/setup-python-deps@v1
        with:
          requirements-files: |
            requirements/tools.txt
          python-version: "3.12"
          require-hashes: true

      - name: Verify generated lock files are current
        run: |
          pip-compile --strip-extras --no-annotate --allow-unsafe --generate-hashes -o requirements/tools.txt requirements/tools.in
          pip-compile --strip-extras --no-annotate --allow-unsafe --generate-hashes -o requirements/base.txt requirements/base.in
          pip-compile --strip-extras --no-annotate --allow-unsafe --generate-hashes -o requirements/dev.txt requirements/dev.in
          git diff --exit-code -- requirements/base.txt requirements/dev.txt requirements/tools.txt

      - name: Install development dependencies
        uses: validityBase/vbase-github-actions/.github/actions/setup-python-deps@v1
        with:
          requirements-files: |
            requirements/dev.txt
          python-version: "3.12"
          require-hashes: true

      - name: Check installed dependencies
        run: python -m pip check
