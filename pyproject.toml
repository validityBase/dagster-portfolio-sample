[project]
name = "dagster-portfolio-sample"
version = "0.1.0"
description = "Sample Dagster and vBase pipeline for a simple portfolio"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache 2.0" }
authors = [
    { name = "ValidityBase", email = "info@validitybase.com" }
]

[tool.isort]
# Avoid conflicts with black formatter.
profile = "black"

[tool.black]
target-version = ["py312"]

[tool.dagster]
module_name = "dagster_pipelines.definitions"
